// TODO: Esercizio di comparazione

let numberOne = 1;
let stringOne = "1";

let numberThirty = 30;
let stringThirty = "30";

let numberTen = 10;
let stringTen = "10";

// ? Primo Esercizio
   if (numberOne == stringOne)  {
      console.log("Le variabili numberOne e stringOne hanno lo stesso valore, ma il type diverso.");
   } else {
      console.log("Le variabili numberOne e stringOne non hanno lo stesso valore");
   }

// ? Secondo Esercizio

if (numberThirty === stringThirty)  {
   console.log("Le variabili numberThirty e stringThirty hanno lo stesso valore e lo stesso type.");
} else {
   console.log("Le variabili numberThirty e stringThirty non hanno lo stesso type");
}

// ? Terzo Esercizio

if (numberTen === stringTen)  {
   console.log("Le variabili numberTen e stringTen hanno lo stesso valore, ma il type diverso.");
} else {
   console.log("Le variabili numberTen e stringTen non hanno lo stesso valore");
}